# data

Static data for the data-science site.

- `seed` content moved here — curated rules + watchlists (markets thresholds, news topics).
- `snapshots/` — crawler output: JSON per category, committed by `collect.yml` and rendered by the static `ui/`.
